# Pipeline Atlas registration

Hasmukh's requested placement is represented as:

`domain-warehouse -> raw-materials -> lifecycles -> existing Assembler/Belt/Provision path`

This repository does not modify the current downstream engines. The host/control-plane should register this manifest when Pipeline Atlas integration is enabled.
