{
  "name": "malkom-agenticai-engine",
  "private": true,
  "version": "0.1.0",
  "description": "Malkom Agentic AI engine. Agents get a goal, not a list of steps. One contract, one runner, one MCP gateway, adapters per framework.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/contract packages/gateway packages/adapters packages/runner packages/studio",
    "clean": "tsc -b --clean packages/contract packages/gateway packages/adapters packages/runner packages/studio",
    "typecheck": "tsc -b packages/contract packages/gateway packages/adapters packages/runner packages/studio",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
