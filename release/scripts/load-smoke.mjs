const base=process.env.PILOT_BASE_URL;if(!base)throw new Error('PILOT_BASE_URL required');const qs=['What happens at origin receipt?','How is freight rated?','What happens after delivery?','What evidence closes custody?'];const times=[];for(const question of qs){const t=performance.now(),r=await fetch(base.replace(/\/$/,'')+'/api/ask-atlas',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({question})});if(!r.ok)throw new Error('ask failed');await r.json();times.push(performance.now()-t)}times.sort((a,b)=>a-b);console.log(JSON.stringify({requests:times.length,p95Ms:times[Math.ceil(times.length*.95)-1],times},null,2));
