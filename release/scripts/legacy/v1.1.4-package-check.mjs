import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';
const root=path.resolve(new URL('../..',import.meta.url).pathname),sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex'),checks=[],add=(n,x,d='')=>checks.push([n,!!x,d]);
for(const ch of ['stable','lab']){const d=path.join(root,'release/packages',ch);add(`${ch}: index exact`,sha(path.join(d,'index.html'))===sha(path.join(root,'index.html')));for(let i=17;i<=21;i++)add(`${ch}: Stage${i} donor exact`,sha(path.join(d,`stage${i}-client.js`))===sha(path.join(root,`stage${i}-client.js`)));add(`${ch}: exactly 8 functions`,fs.readdirSync(path.join(d,'api')).filter(x=>x.endsWith('.js')).length===8);const idx=fs.readFileSync(path.join(d,'index.html'),'utf8');add(`${ch}: spatial field/mesh/minimap`,idx.includes('.field{')&&idx.includes('ambient-mesh152')&&idx.includes('mini-map'));add(`${ch}: boxed renderer absent`,!idx.includes('.territory{width:150px'));add(`${ch}: v1.1 contract`,JSON.parse(fs.readFileSync(path.join(d,'data/modules/road-ltl-v1.2.json'),'utf8')).contractVersion==='atlas-data-contract-v1.1')}
add('stable: AP standalone excluded',!fs.existsSync(path.join(root,'release/packages/stable/accounts-payable-fixture-standalone.html')));add('lab: AP standalone retained',fs.existsSync(path.join(root,'release/packages/lab/accounts-payable-fixture-standalone.html')));add('lab: evaluation workbench retained',fs.existsSync(path.join(root,'release/packages/lab/pilot/evaluation-harness.html')));
for(const[n,ok,d]of checks)console.log(`${ok?'PASS':'FAIL'} · ${n}${d?' · '+d:''}`);if(checks.some(x=>!x[1]))process.exit(1);
