import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
const root=path.resolve(new URL('../..',import.meta.url).pathname);
const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const walk=d=>{const out=[];for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())out.push(...walk(p));else out.push(p)}return out};
const runtimeRoots=['api','lib/api','engine','data','governance'];
let rels=['index.html','stage17-client.js','stage18-client.js','stage19-client.js','stage20-client.js','stage21-client.js','package.json','vercel.json','.env.example','release/release-meta.js','release/baselines/v1.1.6-critical-hashes.json'];
for(const d of runtimeRoots)rels.push(...walk(path.join(root,d)).map(p=>path.relative(root,p)));
rels=[...new Set(rels)].sort();
const files=rels.map(rel=>({rel,sha256:sha(path.join(root,rel)),bytes:fs.statSync(path.join(root,rel)).size}));
const critical=JSON.parse(fs.readFileSync(path.join(root,'release/baselines/v1.1.6-critical-hashes.json'),'utf8'));
const manifest={schemaVersion:'scoip-release-manifest-v1',releaseId:'scoip-v1.1.6-release-certified-2026.08.24',version:'1.1.6',releaseType:'RELEASE_CERTIFIED_FULL_PRODUCT_PARITY',lineage:{frontend:'Stage 23 golden spatial/full-feature runtime',foundation:'Foundation Hardening v1.1',deployment:'v1.1.3 8-function Vercel consolidation pattern',integrityGuards:'v1.1.5',releaseCertification:'v1.1.6'},contracts:{parity:'stage23-full-product-parity-v1',atlasData:'atlas-data-contract-v1.1',domain:'enterprise-domain-extension-v1.0'},nodeEngine:'24.x',vercelFunctions:fs.readdirSync(path.join(root,'api')).filter(x=>x.endsWith('.js')).length,legacyApiPaths:JSON.parse(fs.readFileSync(path.join(root,'vercel.json'),'utf8')).rewrites.filter(x=>x.source.startsWith('/api/')).length,generatedAt:new Date().toISOString(),criticalIntegrity:critical,files};
fs.writeFileSync(path.join(root,'release/v1.1.6-release-manifest.json'),JSON.stringify(manifest,null,2));
console.log(JSON.stringify({ok:true,releaseId:manifest.releaseId,files:files.length,vercelFunctions:manifest.vercelFunctions,legacyApiPaths:manifest.legacyApiPaths},null,2));
