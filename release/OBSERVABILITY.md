# Observability

Use Vercel logs + health/readiness endpoints for runtime. Workspace telemetry stores only allow-listed event types and scalar metadata; prompts and document text are excluded. Monitor error rate, Ask Atlas latency, document-structure failures, auth/RLS denials and pilot hard-failure counts.
