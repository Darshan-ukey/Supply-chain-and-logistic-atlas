# Pilot Security Mode

Raw uploaded documents and derived raw representations are session-ephemeral. Pilot users can create work-state but cannot publish canonical Atlas changes. Database authorization/RLS is the client-data security boundary. Provider retention/training controls must be independently approved before confidential testing.
