# Atlas V2.1 Execution Fabric — Build Audit (dev1)

## Verdict

**Architecture frozen; first implementation slice built and regression-tested.**

This is a development build, not a production promotion.

## Implemented in dev1

1. Frozen `Atlas Execution Fabric Architecture V1`.
2. Executor-neutral `Work Decomposition V1.1` contract.
3. Minimal `Atlas Runtime Adapter Standard V1` contract.
4. Runtime projection contract and Malkom adapter manifest.
5. Runtime adapter registry/capability negotiation core.
6. First Atlas-owned `MalkomAdapterV1` implementation.
7. Complete Malkom 3.0 engine suite re-homed as an Atlas runtime family under `execution/runtimes/malkom/engine-suite`.
8. Atlas Execution Flow Generator:
   - Canonical flow from WorkDefinition/decomposition semantics;
   - Malkom projection flow;
   - BPMN and simplified Flow rendering from the same graph;
   - guarded finite path enumeration;
   - STAY return edges;
   - explicit runtime-gap edges;
   - BPMN 2.0 XML export;
   - SVG export.
9. Admin WorkDefinition UI now has a sixth `Flow Generator` tab and Inspector/Work Library entry actions.
10. Ask Atlas command routing recognizes flow/BPMN requests in Admin.
11. Capability-oriented backend foundation:
   - capability resolver;
   - Malkom view/configure/compile/materialize/operate capability vocabulary;
   - `/api/runtime-access`;
   - `/api/malkom-projections`;
   - no new top-level Vercel function.
12. Protected Execution Fabric database migration:
   - adapters;
   - runtime projections;
   - client bindings;
   - client extensions;
   - materializations;
   - runtime evidence;
   - user capabilities.
13. Generic runtime-access browser shell: authenticated users with `RUNTIME_MALKOM_VIEW` can receive the Malkom entry point and browse protected Malkom projections without becoming Atlas Admin.
14. Protected execution source is excluded from Vercel static deployment through `.vercelignore`.

## Validation results

### Canonical non-regression

PASS:

- frozen Page 0 HTML hashes unchanged;
- frozen Road LTL HTML hashes unchanged;
- Page 0 machine-readable JSON unchanged;
- Road LTL machine-readable JSON unchanged;
- 22 Road LTL processes;
- 13 A3 parents;
- 39 process-flow edges;
- 22 execution transitions;
- 29 sources;
- 27 lenses;
- 15 child actors;
- 176 ontology edges.

### Public IP boundary

PASS:

- public Atlas still excludes detailed WorkDefinition/runtime tokens;
- Admin HTML shell still excludes embedded protected WorkDefinition payload;
- runtime source/contracts/adapters are excluded from Vercel deployment;
- protected data remains server-retrieved.

### Flow generator smoke

Across all 22 Road LTL WorkDefinitions from the owned Malkom runtime fixture:

- 22 canonical graphs generated;
- 22 Malkom graphs generated;
- 22 BPMN exports generated;
- 62 STAY return edges retained in generated queue/subqueue paths;
- 120 finite paths terminated by loop guards rather than infinite traversal;
- 11 runtime-gap/non-supported edges remain visible;
- Malkom adapter registered and assessed all definitions.

### Malkom embedded runtime audit

Existing v0.2.4 deep runtime audit remains PASS at the contained engine boundary.

It still reports the known runtime capability boundary for three canonical escalations (`LTL-15`, `LTL-18`, `LTL-22` → `LTL-14`) rather than flattening them.

### Browser

Chromium desktop + mobile PASS:

- zero runtime errors;
- zero horizontal overflow;
- existing primary Atlas navigation retained;
- public V2 remains 3 safe detail tabs;
- Admin V2 now exposes 6 protected detail tabs;
- Admin Flow Generator renders a generated canonical graph.

## Protected files changed

No canonical source/content file was changed.

Product/runtime files changed or added include:

- `admin.html` — Flow Generator integration;
- `index.html` — generic capability-gated runtime shell only;
- `lib/api/_utils.js` — capability resolution;
- `api/atlas.js` — two new actions under existing consolidated function;
- `vercel.json` — two rewrites;
- `.env.example` — capability bootstrap variables;
- `.vercelignore` — protected execution-source exclusion;
- `migrations/v2.1-execution-fabric.sql`;
- new `execution/` architecture/runtime tree.

## Explicitly pending — not misrepresented as complete

1. Populate true Work Decomposition V1.1 elements for every Atlas WorkDefinition; current V2 definitions remain compatible but are not all yet regenerated into the richer executor-neutral topology contract.
2. Move Atlas Warehouse persistence from the current single WorkDefinition table into full lineage/dependency-aware governed assets where needed.
3. Implement runtime projection generation server-side from canonical Atlas WorkDefinitions rather than relying on already-present Malkom projections for the first proof.
4. Build capability management/governor UI for granting/revoking runtime capabilities.
5. Build Malkom configure/client-binding UI for non-Admin Malkom users.
6. Wire compile/materialize/deploy operations to the actual Malkom Command/control-plane host when that host endpoint is available.
7. Add runtime outcome/evidence return connector and canonical reconciliation.
8. Add Canonical ↔ Projection ↔ Materialized/Actual visual diff.
9. Add other adapters only when a real target runtime is selected; do not invent generic pseudo-runtime models.
10. Production migration/application/deployment has not been performed.

## Release status

`v2.1.0-dev.1` = **BUILD STARTED / ARCHITECTURE FROZEN / NOT PRODUCTION PROMOTED**.
