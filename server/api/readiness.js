'use strict';const {json,supabase}=require('./_utils');module.exports=async(req,res)=>{const checks={release:true,supabase:false,llm:String(process.env.ATLAS_REQUIRE_LLM||'false')!=='true'||Boolean(process.env.ATLAS_LLM_API_KEY)};if(process.env.SUPABASE_URL&&process.env.SUPABASE_PUBLISHABLE_KEY){try{await supabase('/rest/v1/atlas_workspaces?select=id&limit=1');checks.supabase=true}catch(e){checks.supabase=false}}const ok=checks.release&&checks.supabase&&checks.llm;json(res,ok?200:503,{ok,checks,note:checks.supabase?'Dependencies ready':'Supabase schema/configuration is required for authenticated pilot workspaces.'})};
