'use strict';const {json,body,setCookie,supabase,safeError}=require('./_utils');module.exports=async(req,res)=>{if(req.method!=='POST')return json(res,405,{ok:false});try{const b=await body(req);const d=await supabase('/auth/v1/signup',{method:'POST',body:{email:b.email,password:b.password}});if(d.access_token)setCookie(res,'atlas_access_token',d.access_token,{maxAge:d.expires_in||3600});json(res,200,{ok:true,user:d.user,sessionCreated:Boolean(d.access_token),message:d.access_token?'Account created and signed in.':'Account created. Confirm email if required, then sign in.'})}catch(e){json(res,e.status||400,{ok:false,error:safeError(e)})}};
