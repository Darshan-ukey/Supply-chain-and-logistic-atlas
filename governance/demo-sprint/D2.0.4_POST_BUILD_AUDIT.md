# D2.0.4 — Malkom 3.0 Adapter / Projection Integration — POST BUILD AUDIT

**Stage:** D2.0.4
**Executor:** Claude (backup executor, continuing under Owner direction after D2.0.3 handover)
**Branch:** `atlas-v2-demo-2026-09-14`
**Build commit:** `457003d`
**Prior stage state:** `8184f11` (D2.0.3 closure)
**Queue scope:** `WIRE_AND_DEMONSTRATE_VERIFIED_EXISTING_MALKOM_ADAPTER_PROJECTION_FROM_PROVEN_REFERENCE_LINEAGE`
**Date:** 2026-09-12

---

## 1. What was built

| Artifact | Purpose |
|---|---|
| `tools/malkom-projection/build-malkom-reference-projection.mjs` | Deterministic generator |
| `data/materialized/road-ltl-v2.3-malkom-reference-projection.json` | Committed projection artifact |
| `tests/d2-0-4-malkom-reference-projection.mjs` | Certification suite |

The scope was *wire and demonstrate the existing verified adapter* — not build a new one. The
adapter itself (`execution/adapters/malkom/malkom-adapter.mjs`) was imported byte-identical at
`f22b77d` and is **not modified** by this stage. This stage runs it and commits its output so the
demo shows a real artifact rather than a live-computed claim.

---

## 2. Results

| Metric | Result |
|---|---|
| definitions processed | 22 |
| adapter-compatible | **22 / 22** |
| materializable | **22 / 22** |
| client bindings required | 176 across 8 families |
| ESCALATE PARTIAL tasks | 3 (LTL-15, LTL-18, LTL-22) |
| adapter operations not enabled | 5 |
| semantic hash | `d077166f…` |

---

## 3. Guardrail compliance

### `NO_FALSE_V15_COMPILER_CLAIM`
The artifact declares `classification: DEMO_REFERENCE_PROJECTION_NOT_CANONICAL_TRUTH`,
`sourceModel: "Road LTL V1.2"`, chain `V1.2 -> Domain Warehouse v2.3 -> Malkom 3.0`, and an
explicit `notGeneratedFrom` list naming Road LTL 1.5, Operational Knowledge v2, the P6.1
decomposition and the P6.2 compiler.

Enforced mechanically, not just documented: the generator reads exactly three files, all
reference-lineage, and the test asserts no declared input path matches governed-target-lineage
patterns (`road-ltl-v1.[45]`, `operational-knowledge`, `work-decomposition`, `work-definition`,
`p6-`). A future change that starts reading 1.5/P6.x data **fails the suite**.

### `MALKOM_IS_FIRST_CONSUMER_NOT_CANONICAL_ATLAS_MODEL`
Classification and lineage statement both assert the artifact is a downstream consumer
projection, not canonical Atlas truth. The source bundle's own note is carried through verbatim.

### `DOCUMENT_KNOWN_ADAPTER_LOSSES_GAPS`
Three real limitation classes recorded, none suppressed:
- **ESCALATE** — `PARTIAL`, three tasks named individually, with the reason (live cross-queue
  materialization is runtime-specific).
- **Client binding** — `CLIENT_BINDING_REQUIRED`, 176 points across 8 families with per-family
  counts, stating Atlas owns the requirement and the client supplies the value. None resolved,
  none invented.
- **Adapter operations not enabled** — `materialize`, `deploy`, `execute`, `status`,
  `reconcileEvidence` reported honestly as unavailable.

### `NO_VERCEL_DEPLOYMENT`
No Vercel action occurred.

---

## 4. Verification

**Determinism:** byte-identical on regeneration; stable semantic hash; zero wall-clock values;
independent in-memory rederivation agrees.

**Negative controls** — the guards were proven to fail, not merely to pass:
- tampering `sourceModel` to `"Road LTL 1.5"` → test **FAILS** as designed
- emptying the ESCALATE `affectedTasks` list → test **FAILS** as designed

Both tampered states restored and a clean run re-confirmed.

**Source integrity:** reference bundle and governed adapter asserted unmodified by hash.

---

## 5. Stage result

**D2.0.4 PASS at structural / source / execution level.**

Known limitation carried forward unchanged: browser/visual click-through remains **open**. No
browser or headless driver is available in the executor environment and Vercel mutation is
prohibited. Must be closed by human pre-demo visual QA.

No canonical data, module catalog, production pointer, API security boundary, Vercel
configuration or frozen historical artifact was mutated.

**Safe resume point:** `atlas-v2-demo-2026-09-14` @ `457003d` (this audit commit follows).
