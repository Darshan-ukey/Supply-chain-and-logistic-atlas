# Claude DUX rendered-failure remediation handoff — 2026-09-13

Classification: OWNER_OBSERVED_RENDERED_FAILURE + OWNER_AUTHORIZED_REMEDIATION
Demo branch: `atlas-v2-demo-2026-09-14`
Failed candidate commit: `482f2673642df81911ee0815a04ff402f8525d07`
Failed candidate deployment: `dpl_8EEXBrHKVFwGVtg1KajAPza77Ndx`
Failed candidate preview: `https://logisticatlasv2-5hizj2zgt-ukeydarsh-2051s-projects.vercel.app`

The Owner manually tested the exact preview with a click-capable browser. Treat this as definitive rendered evidence. DUX-01 and DUX-02 are actual rendered failures. DUX-03 technically renders internal-demo content, but the current content is insufficient for the intended stakeholder/Malkom demonstration and therefore fails the functional demo objective.

## DUX-01 — FAILED_RENDERED
Owner observation: after selecting a Road LTL A5 task on Canvas, **no `Open Execution Depth` option is visible in the Inspector**.

Required remediation:
1. Trace the actual Inspector render path used by the live Stage 21 Canvas. Do not assume the code path modified in `482f267` is the one rendering the selected-task Inspector.
2. Add a visible, discoverable `Open Execution Depth` button/action to the actual rendered A5 Inspector for Road LTL selected tasks.
3. Route dynamically to `/daughter?moduleId=road-ltl&moduleVersion=1.5&taskId=<selectedTaskId>&internalDemo=1` for this internal demo build so the stakeholder lands directly in the usable internal-demo execution-depth surface. Do not hard-code `LTL-03`.
4. Keep existing Inspector controls intact.
5. Verify in a real browser that the button is visible for selected Road LTL A5 tasks and absent where not applicable.

Closure criterion: Owner/browser can see the button on the live Canvas and activate it into the correct Daughter task.

## DUX-02 — FAILED_RENDERED
Owner observation: **Ocean FCL and Ocean LCL still show as Coming Soon / are not presented as active** on the Universe/Models surface.

Canonical source truth already established: `data/module-catalog.json` on the demo branch lists:
- `ocean-fcl` v0.5 — `publicationState: ACTIVE`, `depth: A5_VERIFIED`, `approved: true`
- `ocean-lcl` v0.5 — `publicationState: ACTIVE`, `depth: A5_VERIFIED`, `approved: true`

Required remediation:
1. Trace the exact rendered Universe/Models source for status badges/cards, including any stale registry, coverage registry, embedded Stage data, or cached presentation object.
2. Fix the **actual live presentation source**, not only helper logic that is not used by the rendered panel.
3. Ocean FCL/LCL must render as active/available and be selectable/openable.
4. Genuine planned domains must remain planned/Coming Soon.
5. Verify rendered state in a real browser.

Closure criterion: Owner/browser sees Ocean FCL and Ocean LCL as available/active on the Universe/Models surface and can open them.

## DUX-03 — FUNCTIONALLY INSUFFICIENT FOR DEMO
Owner manually clicked both internal-demo tabs. The authorization block is removed, so the toggle works, but the resulting content is too shallow for the intended demonstration.

Observed Work Decomposition content currently shows only aggregate counts, e.g. overall 603 work units / 444 leaves / 185 executor-ready / 163 client-binding blocked / 96 knowledge-gap blocked and LTL-03 aggregate 43/37/14/8/15. This does **not show what the decomposition actually contains**.

Observed WorkDefinition content currently shows compiler certification and zero persisted rows plus expected counts. This does **not show the WorkDefinition structure or what a runtime such as Malkom can consume**.

Required remediation — internal demo branch only:
1. Preserve the explicit `INTERNAL DEMO VIEW — not production authorization` banner and reversibility.
2. Work Decomposition must expose a readable, drillable/sample **actual decomposition tree or rows for the selected task** (LTL-03 is sufficient for demo), not only totals. Show at least:
   - hierarchy/levels or parent-child work units;
   - work-unit IDs/names;
   - terminal/non-terminal indication;
   - readiness/block status;
   - relevant knowledge/client-binding dependency where available.
3. WorkDefinition must expose a **representative canonical WorkDefinition payload/sample structure** for selected executor-ready leaf/leaves generated by the proven compiler path, clearly labeled as `compiler output sample / not persisted` if persistence is still zero. Show the fields a runtime consumer can use, such as identity, trigger/preconditions, inputs, outputs, rules/controls, action, timing/SLA where present, evidence, exceptions/HITL/client-binding/runtime-mapping status. Use only fields actually supported by the governed WD/compiler artifacts; do not invent fields.
4. Add an explicit `Runtime consumption / projection` section showing how this canonical WD can be projected to Malkom **without claiming the current v1.5 WD produced the old proven Malkom v1.2 reference implementation**. Recommended wording: `Canonical WorkDefinition is the runtime-neutral specification. Malkom is one adapter/consumer pattern; the proven reference projection shown elsewhere remains the separate v1.2 → Domain Warehouse v2.3 lineage.`
5. If a real compiled dry-run WD artifact already exists in repository history/governance, use that exact artifact/sample. If only compiler proof exists and no saved sample exists, first run/inspect the existing compiler in dry-run mode and persist a demo-safe sample artifact on the demo branch; do not fabricate a hand-written pseudo-WD.
6. Keep default Daughter without `internalDemo=1` fail-closed/protected.

Closure criterion: from the internal-demo Daughter, a stakeholder can click Work Decomposition and understand the actual breakdown of LTL-03, then click WorkDefinition and see a real runtime-neutral specification/sample that makes it obvious what Malkom/agents/BPM/ERP adapters can consume.

## Mandatory execution order
1. Log PRE_ACTION in `claude_chatGPT.md` before any code change.
2. Fix DUX-01 and rendered-test it.
3. Fix DUX-02 and rendered-test it.
4. Fix DUX-03 and rendered-test it.
5. Run regressions: root Canvas, `/app`, standard Daughter without demo flag, POC Journey, Execution Readiness, 8-router invariant, full-state certification.
6. No main merge or production promotion.
7. Update shared log with exact commit, deployment, URLs, and PASS/FAIL for each item.

Current disposition:
- `DUX-01 = FAILED_RENDERED_OWNER_VERIFIED`
- `DUX-02 = FAILED_RENDERED_OWNER_VERIFIED`
- `DUX-03 = FAILED_DEMO_OBJECTIVE_OWNER_VERIFIED`
- `D2.0.7 = BLOCKED_PENDING_DUX_REMEDIATION`
