# D2.0.3 — Road LTL Execution Depth — POST BUILD AUDIT

**Stage:** D2.0.3 — Road LTL execution-depth explorer
**Executor:** Claude (hot-backup executor; picked up after ChatGPT's D2.0.3 stalled fail-closed)
**Branch:** `atlas-v2-demo-2026-09-14`
**Build commit:** `f22b77d`
**Prior stage state:** `6ef1d41` (ChatGPT D2.0.2 POST_BUILD_AUDIT)
**Date:** 2026-09-12

---

## 1. Why this stage stalled, and what unblocked it

ChatGPT declared D2.0.3 fail-closed at `621d403` after its searches for the v2.3 / Malkom
reference assets returned 404 on the demo branch, and correctly refused to import assets it
could not directly read.

Root cause identified at 16:41 IST: the path is **double-nested** —
`canvas-v2/canvas-v2/data/...`, not `canvas-v2/data/...`. A single-nested path query 404s even
on a branch that genuinely holds the file.

Consequence: **most of what D2.0.3 needed was already on the branch.** The v2.3 reference
WorkDefinition bundle arrived inside the frozen Canvas shell at `45a2888`. Only the Malkom
adapter was genuinely absent.

---

## 2. What this stage changed

Imported from `atlas-presentation-architecture-v1-p6-2` @ `6ae0035`, byte-identical to source
(verified `git hash-object` against the origin blob):

| File | Status |
|---|---|
| `execution/adapters/malkom/malkom-adapter.mjs` | imported, byte-identical |
| `execution/contracts/malkom-adapter-manifest-v1.json` | imported, byte-identical |

No other file was added, modified or deleted.

---

## 3. Verification by execution

The adapter was run against all 22 real reference WorkDefinitions, not inspected:

| Metric | Result |
|---|---|
| definitions processed | 22 |
| adapter-compatible | **22 / 22** |
| materializable | **22 / 22** |
| client bindings required | 176 (flagged `CLIENT_BINDING_REQUIRED`) |
| PARTIAL warnings | 3 (`ESCALATE`) |
| `nextStep` kinds present | `END_WORK_ITEM`, `STAY_IN_QUEUE`, `ESCALATE` |

The 3 PARTIAL warnings are honest reporting, not failures: the adapter marks `ESCALATE` as
runtime-specific for live cross-queue materialization while keeping canonical escalation visible.

---

## 4. Two-lineage separation — the decisive guardrail

**Governed target lineage** (already present before this stage):
Road LTL 1.5 → Operational Knowledge → certified P6.1 recursive decomposition →
canonical WD compiler proven, **persistence PENDING**.
Served by `/api/execution-depth-projection` + `road-ltl-1.5-public-safe-projections.json.gz.b64`.

**Proven reference lineage** (completed by this stage):
Road LTL **V1.2** → Domain Warehouse **v2.3** → Malkom 3.0 projection.
The bundle self-declares `sourceModel: "Road LTL V1.2"`, `source:
"Malkom-Domain-Warehouse-Engine-V2.3-Lossless"`, and `note: "Protected/admin prototype
projection. Canonical Atlas A5 semantics remain authoritative; these definitions are additive
derived execution assets."`

**These lineages are not connected, and this stage does not connect them.**

Explicitly NOT claimed:
- that the Malkom projection was generated from Road LTL 1.5 or P6.2;
- that the 185 EXECUTOR_READY leaves are persisted canonical WorkDefinitions
  (`atlas_work_definitions` remains 0 rows);
- any Client Binding or runtime-projection closure — 176 bindings remain explicitly required.

---

## 5. Regression

P4 integration suite: one pre-existing failure, unchanged from `45a2888` — root `index.html`
byte-identity against the P3/P3O presentation-lineage baseline, which this `main`-cut branch was
never expected to match. **Not a regression from this stage.**

All Canvas V2 frozen-package byte-identity gates pass. All Ocean and Road LTL semantic gates pass,
including `ocean-lcl@0.6 has no semantic fallback to Ocean 0.5`.

---

## 6. Stage result

**D2.0.3 PASS at structural / source / execution level.**

Known limitation carried forward unchanged: browser/visual click-through remains **open**. No
browser or headless driver is available in the executor environment and Vercel mutation is
prohibited. Must be closed by human pre-demo visual QA.

No canonical data, module catalog, production pointer, API security boundary, Vercel
configuration or frozen historical artifact was mutated. No Vercel action occurred.

**Safe resume point:** `atlas-v2-demo-2026-09-14` @ `f22b77d`.
