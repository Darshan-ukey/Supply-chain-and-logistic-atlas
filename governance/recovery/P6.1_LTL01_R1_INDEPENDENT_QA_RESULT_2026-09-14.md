# P6.1 V1 LTL-01 R1 — Independent QA Result

**Reviewer:** Claude
**Candidate:** `governance/baselines/P6_1_V1_LTL01_RECONSTRUCTED_R1.json` @ `407c099`
**Self-QA reviewed as evidence-to-verify:** `f5ccc8e`
**Rule authority applied:** frozen CR1–CR11 @ `3c67bb4`

Historical LTL-01 (23/16/7/9/0) used as post-hoc comparison only, per instruction — checked after content review, not as a QA target.

---

## Source custody — independently re-verified, not trusted from the certification document

Downloaded the actual Drive ZIP (`1CVUC40CZuhexs8oJjasBFw7OAjv4AhUI`) directly and recomputed its SHA-256 myself: **matches** the certified `b81b22d2...` exactly. Extracted and independently hashed all four claimed source files (`road-ltl-v1.4.json`, `-operational.json`, `-claims.json`, `-bindings.json`): **all four match** the certification's declared hashes. Read LTL-01's actual entries in all four files directly, before reading the reconstructed candidate, to avoid being anchored by its framing.

## Structural/schema/graph — independently recomputed

Schema validation against the real schema file: **PASS**. Graph recomputed from raw JSON: 19 units, 15 leaves, 4 internal nodes (1 root + 3 composites), exactly 1 root, 0 duplicate IDs, 0 bad parent references, 0 cycles, 0 leaves stuck at `NEEDS_DECOMPOSITION`. Leaf statuses recomputed: 7 `EXECUTOR_READY` / 8 `BLOCKED_BY_CLIENT_BINDING` / 0 `BLOCKED_BY_KNOWLEDGE_GAP`. **All match the candidate's declared summary exactly**, and the summary itself is schema-conformant (no repeat of the R2-round manifest gap).

## Checkpoint 2 — the 3 composite containers are source-disciplined, not artificial

Each container maps 1:1 to a named source array, verified directly:
- `INFO` (8 children) = the source's 8-entry `requiredInformation` array, exactly.
- `GATES` (4 children) = the source's 3 `decisionGates` + 1 `temporalConstraints` entry.
- `OUTCOME` (2 children) = the source's single `atomicActions` entry + its 2 `evidenceContracts`, correctly consolidated into one evidence-capture leaf rather than split — correct CR2 discipline (CR2 explicitly names "evidence-list items" as insufficient grounds for separate children).

`AUTH`, standing alone rather than nested, corresponds to its own distinct, independently source-supported binding record (`CB::LTL-01::04`) — justified as its own unit under CR2, not a lexical split from an adjacent group.

## Checkpoint 3 — all 7 ready leaves checked against CR1–CR11

Read all 7 in full. CR10 treatment is genuinely well-differentiated, not uniform:
- `I01`–`I04` (information resolution) and `O02` (evidence capture): `NOT_MATERIAL`, each with a leaf-specific reason (e.g. *"does not decide service eligibility"*, *"records already-produced governed evidence"*).
- `G01` (completeness gate): `NOT_MATERIAL` — correctly distinguished from `G02`/`G03`, which are genuinely client-configured per source (`clientConfigured: true`) and remain blocked.
- **`O01` (the state-changing action): `MATERIAL_AND_RESOLVED`** — and resolved correctly, citing the exact source fields I independently confirmed exist verbatim on `LTL-01::ACT::01`: `performerRole`, `authorityOwner` ("Contracted service owner; controlled override authority"), `authorityObject`, `authoritySystem`. This is a more sophisticated and correct application of CR10 than the LTL-03 R2 worked example, which recorded `NOT_MATERIAL` uniformly across all 14 ready leaves — here, the one leaf where authority is genuinely material is correctly identified and resolved rather than defaulted to not-material.

CR11 (failure/alternate path): `O01`'s `outputState` is a 3-value array (`SERVICE_REQUEST_ELIGIBLE`/`CONDITIONAL`/`REJECTED`), directly matching the source's `expectedOutcomes` — genuine alternate-path representation, not success-only.

## Checkpoint 4 — all 8 client-binding leaves checked against actual v1.4 source

`I05`–`I08` map directly to `REQ::05`–`08`, all four independently confirmed `clientBindingRequired: true` in source. `G02`/`G03` map to `DG::02`/`DG::03`, both independently confirmed `clientConfigured: true`. `G04` maps to the temporal constraint, independently confirmed `resolutionStatus: CLIENT_OR_REGIME_BINDING_WHERE_NOT_SOURCE_EXPLICIT`. `AUTH` maps to `CB::LTL-01::04`, independently confirmed `resolutionStatus: CLIENT_ORG_AUTHORITY_REQUIRED`. **Every one of the 8 traces to an explicit, directly-verifiable client-level marker in frozen source** — none appear invented or misclassified.

## Checkpoint 5 — `LTL-01::REQ::05` as the commodity-classification binding reference

Verified this is a deliberate, consistent choice, not an inconsistency. Checked how the other blocked leaves cite their references: `I06`/`I07`/`I08`/`AUTH` all correctly use the formal `CB::LTL-01::0X` records, which have clean 1:1 matches (service catalogue, cut-off rules, contract entitlements, override authority respectively). **No formal `CB::` record is an exact match for "commodity classification reference"** — the closest, `CB::LTL-01::03`, is about *prohibited-commodity policy*, a related but distinct concept. `I05` correctly falls back to the directly-marked `REQ::05` (`clientBindingRequired: true` in source) rather than force-fitting a non-matching formal record. `G03` independently cross-references `LTL-01::REQ::05` alongside `CB::03`/`CB::05` for the same underlying reason, confirming the identifier is used consistently, not ad hoc. **Valid as represented; no change needed.**

## Checkpoint 6 — `WD-LTL01-R1-O01`'s CR6 standing

Independently confirmed: `LTL-01::ACT::01` in source carries explicit `precondition`, `performerRole`, `authorityOwner`, `targetSystemRole`, and `postcondition` — all five elements present verbatim, not inferred. This is the opposite of the pattern CR6 exists to prevent (a mechanical step assumed ready merely because it follows a resolved binding) — `O01` is independently, fully specified as its own execution contract; its readiness does not depend on the *values* of the upstream client-bound fields, only on the gate *mechanism* existing, which source fully provides. **`O01` correctly remains ready under CR6.**

## Checkpoint 7 — is zero knowledge-gaps substantively justified?

Yes. Every blocked leaf traces to an explicit, named *client-level* configuration/value gap (which carrier network, which cut-off value, which prohibition policy, which contract, which delegated authority) — never to an unresolved *canonical* meaning. This is a materially different situation from LTL-03, where genuine canonical ambiguities existed in source (e.g. undefined precedence order, unexplained field semantics). LTL-01's v1.4 source is comprehensively specified at the canonical level — trigger, decision, rule, control, action, evidence, outcome, and all branch transitions are explicit — leaving only *whose* values apply, which is squarely `BLOCKED_BY_CLIENT_BINDING` territory under CR5/CR8. No canonical gap was found in source that the candidate incorrectly omitted from the knowledge-gap set.

## Checkpoint 8 — structural delta vs. historical, explained after content review

Recomputed delta: -4 units, -1 leaf, 0 ready-count delta, -1 client-binding ref, 0 knowledge-gap, vs. historical `23/16/7/9/0`. Given the content-level findings above (every unit source-disciplined, no fabricated semantics, no misclassified blockers), the smaller unit count plausibly reflects tighter CR1/CR2 discipline (no lexical splitting) rather than any missing coverage — but I note this as a plausible explanation consistent with the evidence, not a proven one; a unit-by-unit historical comparison was not performed and is not required by this QA's scope. The exact 7=7 ready-count match is noted but, consistent with CR9 and the same standard applied in the LTL-03 rounds, is not treated as validating evidence on its own — the content-level checks above are what the disposition rests on.

---

## Disposition

**`P6_1_V1_LTL01_R1_INDEPENDENT_QA_PASS__READY_FOR_REPEATABILITY_FREEZE`**

All 8 required checks independently verified against the actual source and artifact — source custody re-confirmed from the raw Drive archive, not trusted from the certification document; structural/schema/graph recomputed from scratch; every ready and blocked leaf checked individually against verified source content, not against the self-QA's narrative. No defect found at any checkpoint — this is the cleanest of the three independent QA rounds performed in this reconciliation (LTL-03 R1 failed on incomplete executability evidence; LTL-03 R2 failed on an unrecorded CR10 determination; LTL-01 R1 passes on both dimensions with genuine, well-differentiated content, including a materially more sophisticated CR10 application than the LTL-03 worked example).

No Supabase mutation. No WorkDefinition persistence. No main merge. No production promotion. Repeatability-evidence freeze and any subsequent task remain separate Owner-directed steps.
