# P6.1 V1 LTL-03 Reconstruction — Independent QA Handoff to Claude — 2026-09-14

## OWNER INTENT
The Owner has asked that Claude receive the full context and an exact execution procedure. This is an **independent QA gate** over ChatGPT's reconstructed P6.1 V1 decomposition for `road-ltl@1.5 / LTL-03`.

Your job is **NOT** to trust ChatGPT's reconstruction, counts, labels, or classifications. Recompute and challenge them from the frozen governed sources.

Do not mutate any protected runtime store. Do not merge to `main`. Do not promote production. Do not silently rewrite historical frozen evidence.

---

## 1. WHY THIS EXISTS
The historical P6.1 protected decomposition payload was lost/corrupted in protected runtime custody. Recovery work established:

- historical P6.1 certification existed and was valid at the time;
- historical aggregate totals were `22 tasks / 603 work units / 444 leaves / 185 EXECUTOR_READY / 163 BLOCKED_BY_CLIENT_BINDING / 96 BLOCKED_BY_KNOWLEDGE_GAP`;
- the original private P6.1 source bundle was **not found in Drive custody**;
- the single aggregate `BROTLI_BASE64` representation appears to have been the intended certified shipped representation rather than an accidental compression choice;
- the current stored `__ALL_22__` row fails under the authoritative certified Brotli resolver and is therefore a real protected-store data-integrity defect;
- exact byte-for-byte recovery of the lost bundle is therefore not currently possible;
- however, the **frozen architecture, semantic sources, Work Decomposition contract, executability standard and now-frozen V1 compiler specification are sufficient to reconstruct semantically governed decomposition data deterministically**, provided unresolved semantics fail closed and historical aggregate counts are never used as generation targets.

The Owner explicitly accepted the principle that if the architecture for generating Work Decomposition / WorkDefinitions is frozen, semantic regeneration is acceptable; we do not need to pretend to recover lost bytes.

This reconstruction is therefore labelled `RECONSTRUCTED_P6_1_V1`, not historical recovery.

---

## 2. DO NOT RE-OPEN OLD DEAD PATHS
The prior handoff `governance/recovery/P6.1_CORRECT_PATH_HANDOFF_2026-09-14.md` contains an earlier representation-conflict hypothesis and a search for the private bundle. That work is now superseded for this QA task by later findings:

- private bundle search result: `PRIVATE_BUNDLE_NOT_FOUND__CUSTODY_GAP_CONFIRMED`;
- do not spend more time searching Drive for the original private bundle unless new evidence appears;
- do not resume compression guessing;
- do not treat historical totals as compiler targets;
- do not claim the new LTL-03 tree is the historical LTL-03 tree unless independently proven byte-identical, which it is not.

This QA is about whether the new LTL-03 reconstruction is **source-grounded, contract-valid, structurally valid, fail-closed, and faithful to P6.1 V1 semantics**.

---

## 3. EXACT ARTIFACT TO REVIEW
Repository: `Darshan-ukey/Supply-chain-and-logistic-atlas`

### Reconstruction branch
`atlas-p6-1-v1-reconstruction`

### Machine artifact
`governance/baselines/P6_1_V1_LTL03_RECONSTRUCTED_DECOMPOSITION.json`

### Artifact commit
`39ab0b4995291b3dddd664a8d3d13ad2fdc4a92e`

### ChatGPT self-QA evidence commit
`5b87c80fa675af887030016be46b3ddd87f0043d`

Treat self-QA as **evidence to verify**, not authority.

### Current declared machine counts
- work units: `64`
- terminal leaves: `48`
- `EXECUTOR_READY`: `38`
- `BLOCKED_BY_CLIENT_BINDING`: `7`
- `BLOCKED_BY_KNOWLEDGE_GAP`: `3`
- orphan parents: `0`
- cycles: `0`
- terminal `NEEDS_DECOMPOSITION`: `0`

These are **reconstruction counts only**. Do not compare them to the historical full 22-task aggregate as if they must proportionally match. Do not infer historical LTL-03 counts from them.

---

## 4. GOVERNING P6.1 V1 CONTRACT — USE THESE, NOT VNEXT
On `atlas-p6-1-v1-reconstruction`, read these first:

1. `governance/standards/CANONICAL_WORK_DECOMPOSITION_COMPILER_SPEC_V1_FROZEN.md`
2. `governance/standards/CANONICAL_WORK_DECOMPOSITION_CONTRACT_V1_FROZEN.md`
3. `schemas/canonical-work-decomposition-contract-v1.schema.json`
4. `governance/standards/EXECUTABILITY_AND_RECURSIVE_DECOMPOSITION_STANDARD_V1_FROZEN.md`
5. `governance/standards/CLIENT_BINDING_RESOLUTION_PRINCIPLE_V1_FROZEN.md`
6. `governance/standards/EVIDENCE_EPISTEMIC_CLASSIFICATION_V1_FROZEN.md`
7. `governance/standards/INSIDE_OUT_OUTSIDE_IN_OPERATIONAL_RESEARCH_STANDARD_V1_FROZEN.md`

Important rule: **AR0.2 / VNext semantics do not apply to this reconstruction.** This is historical P6.1 V1 semantics only.

The V1 compiler spec explicitly requires:
- exact pinned source lineage;
- deterministic IDs/order;
- no random canonical IDs;
- no fabrication when authority is missing;
- client-specific runtime values remain client-binding blockers;
- unresolved canonical semantics remain knowledge-gap blockers;
- `NEEDS_DECOMPOSITION` only for non-terminal units with children;
- every leaf must end as `EXECUTOR_READY`, `BLOCKED_BY_CLIENT_BINDING`, or `BLOCKED_BY_KNOWLEDGE_GAP`;
- historical totals are evidence only, never generation targets;
- any regenerated output must remain labelled `RECONSTRUCTED_P6_1_V1` unless byte identity is independently proven.

---

## 5. EXACT SOURCE MATERIAL FOR LTL-03
Review the reconstructed tree against the actual frozen governed LTL-03 sources on the reconstruction branch:

1. `data/modules/road-ltl-v1.5.json`
   - exact LTL-03 v1.5 task override / module semantics

2. `data/operational-knowledge/road-ltl-v1.5-operational.json`
   - frozen Operational Knowledge
   - decomposition seed / actions / states / controls / evidence where present

3. `data/operational-knowledge/road-ltl-v1.5-bol-resolution-baseline.json`
   - canonical object model
   - information-resolution semantics
   - unresolved labels/fields
   - client-binding and source-context gaps

4. `data/source-claims/road-ltl-v1.5-bol-resolution-claims.json`
   - source-backed regulatory / industry claims
   - applicability boundaries

Do not add general logistics knowledge, Malkom assumptions, SEFL rules, client mappings or runtime values unless those exact frozen files support them.

---

## 6. WHAT CHATGPT ALREADY CHANGED DURING MACHINE QA
The first human-readable reconstruction had four unresolved items only in a narrative register. ChatGPT corrected this in the machine artifact so they cannot disappear from terminal classification:

### Now explicit knowledge-gap leaves
- `Code`
- `SHC`
- `Related Value`

These must be verified against the frozen source and should remain `BLOCKED_BY_KNOWLEDGE_GAP` only if canonical meaning/authority is genuinely unresolved.

### Now explicit client-binding leaf
- `BOL Type`

This must remain `BLOCKED_BY_CLIENT_BINDING` only if canonical work is understood but the exact active client/runtime vocabulary or mapping is unresolved.

Also already present in the tree are blockers for items such as:
- Handling Unit Line No
- Reference Number Type Full Name
- Shipper Code / party-account semantics
- Instruction Type value set
- Time Critical Details coding
- source-authority precedence
- critical confidence threshold
- target client/runtime schema mapping

You must independently decide whether every blocker is correctly classified, incorrectly classified, unsupported, duplicated, or missing.

---

# 7. CLAUDE — EXACT EXECUTION PROCEDURE

## STEP A — Freeze the review target
Record in your QA log:
- branch = `atlas-p6-1-v1-reconstruction`
- machine artifact commit = `39ab0b4995291b3dddd664a8d3d13ad2fdc4a92e`
- self-QA evidence commit = `5b87c80fa675af887030016be46b3ddd87f0043d`
- target = `road-ltl@1.5 / LTL-03`
- reconstruction class = `RECONSTRUCTED_P6_1_V1`

Do not review an unpinned moving branch state without recording the exact SHA actually inspected.

## STEP B — Recompute structural/schema QA independently
Do not reuse ChatGPT's arithmetic. Parse the JSON yourself and independently verify:

1. JSON parses.
2. Schema validates against `schemas/canonical-work-decomposition-contract-v1.schema.json`.
3. `schemaVersion = atlas-canonical-work-decomposition-v1`.
4. `contractVersion = 1.0.0`.
5. daughter module/version exactly `road-ltl / 1.5`.
6. source task exactly `LTL-03`.
7. exactly one `TASK_ROOT` with `parentWorkUnitId = null`.
8. every non-root parent ID resolves to a real unit.
9. all `workUnitId` values are unique.
10. no cycles.
11. sibling sequence/order is deterministic and non-ambiguous.
12. every `NEEDS_DECOMPOSITION` unit has at least one child.
13. no terminal leaf has `NEEDS_DECOMPOSITION`.
14. every blocked leaf has at least one stable blocker reference in the correct required array.
15. no `EXECUTOR_READY` leaf carries unresolved blocker references.
16. recompute work-unit count, leaf count and readiness counts from the graph.
17. recompute unique required knowledge-gap references and client-binding references.
18. verify declared summary counts match the recomputed graph.
19. verify only V1-allowed unit types and readiness statuses are used.
20. verify no client/runtime-native structures have been embedded into canonical work.

If any structural/schema check fails, stop semantic certification and report `INDEPENDENT_QA_FAIL__STRUCTURAL_OR_SCHEMA` with exact defects.

## STEP C — Source-ground every work unit
For every work unit, verify:

- `name` and `purpose` are supportable from frozen LTL-03 sources;
- claimed trigger/input/decision/action/evidence/output is explicitly or safely derivable from governed source;
- `sourceRefs` actually support the claimed semantics;
- no step has been invented merely because it is common logistics practice;
- no downstream Malkom/client implementation concept has been promoted into canonical business truth;
- regulatory claims remain conditional to their exact jurisdiction/applicability boundaries.

Create a unit-by-unit QA table with at least:
`workUnitId | classification | source-supported? | correct V1 terminal status? | issue | required correction`.

Do not accept broad semantic similarity. If a leaf is more specific than source authority permits, fail it or weaken/block it.

## STEP D — Apply the historical P6.1 V1 terminal test to every leaf
A leaf may be `EXECUTOR_READY` only when frozen source is sufficient, for the declared HYBRID/HITL executor lens, to identify:

1. required input/object;
2. entry/applicability condition;
3. decision/gate logic without undocumented judgement;
4. one unambiguous operation;
5. success/failure condition;
6. required evidence;
7. resulting state/transition;
8. resolved canonical authority/role.

If any of those are materially unresolved:
- use `BLOCKED_BY_KNOWLEDGE_GAP` where canonical semantics/authority are missing;
- use `BLOCKED_BY_CLIENT_BINDING` where canonical work is understood but active enterprise/client/site/runtime values or mappings are missing;
- never leave a terminal `NEEDS_DECOMPOSITION`;
- never promote a blocked leaf to ready just to improve counts.

Explicitly challenge all 38 currently ready leaves. The burden of proof is on `EXECUTOR_READY`.

## STEP E — Audit blocker taxonomy and completeness
Independently inspect the frozen BOL resolution baseline and confirm all unresolved items have a terminal representation somewhere in the tree.

At minimum, explicitly account for:
- Code
- SHC
- Related Value
- Handling Unit Line No
- BOL Type
- Reference Number Type Full Name
- Shipper Code
- Bill To Account Number semantics
- Consignee Account Number semantics
- Instruction Type values
- Time Critical Details coding

Also assess whether the reconstruction-added blockers below are legitimate under V1 and source-grounded:
- source-authority precedence
- critical confidence threshold
- target client/runtime schema mapping

For each item state one of:
- `CORRECT_KNOWLEDGE_GAP`
- `CORRECT_CLIENT_BINDING`
- `SHOULD_BE_EXECUTOR_READY`
- `WRONG_CLASSIFICATION`
- `UNSUPPORTED_BLOCKER`
- `MISSING_FROM_TREE`

## STEP F — Check semantic architecture boundaries
Verify the tree still obeys the current Atlas North Star without retroactively applying VNext semantics:

- Atlas canonical decomposition describes governed business work and execution-ready specification;
- Atlas itself is not the runtime executor;
- client/Malkom/runtime projection occurs only after canonical resolution;
- target-system schemas do not redefine canonical business truth;
- public-safe projections must remain non-reconstructive;
- protected full decomposition remains execution-protected.

If a unit accidentally turns Atlas into the executing system, flag it.

## STEP G — Compare with the human-readable Drive record only as a consistency aid
Drive working record:
`P6.1 V1 — LTL-03 Reconstructed Protected Decomposition — 2026-09-14`
Drive ID: `1cEYN7ZxJsIpaDukEvZMOs1ihT1hBL268jXfYwbp9cv8`

Use it only to spot machine-vs-human drift. The machine JSON on the pinned GitHub commit is the actual QA target.

Known drift already corrected in machine JSON:
- Code / SHC / Related Value explicit knowledge-gap leaves
- BOL Type explicit client-binding leaf

Do not fail the machine artifact merely because the earlier human text omitted those terminal nodes; instead record the human-readable document as needing sync if the machine artifact passes.

## STEP H — Produce one unambiguous disposition
Choose exactly one:

### PASS
`P6_1_V1_LTL03_RECONSTRUCTION__INDEPENDENT_QA_PASS`

Use only if:
- schema/graph checks pass;
- all source refs and semantics pass;
- every ready leaf satisfies the V1 executability test;
- all blockers are correctly classified and complete;
- no unsupported/fabricated semantics remain.

### PASS WITH CORRECTIONS REQUIRED
`P6_1_V1_LTL03_RECONSTRUCTION__INDEPENDENT_QA_PASS_WITH_CORRECTIONS`

Use only for bounded defects that do not invalidate the reconstruction method. List exact unit IDs and exact edits. Do not directly overwrite the reconstruction artifact in the same checkpoint unless the Owner explicitly asks; first report the correction set.

### FAIL
`P6_1_V1_LTL03_RECONSTRUCTION__INDEPENDENT_QA_FAIL`

Use when the tree materially overstates source authority, misuses V1 semantics, has structural/schema failure, or requires broad redesign rather than bounded corrections.

---

# 8. REQUIRED QA OUTPUT
Create a new durable QA record on the governance branch:

`governance/recovery/P6.1_LTL03_INDEPENDENT_QA_RESULT_2026-09-14.md`

It must contain:

1. exact reviewed branch + commit;
2. exact governing contract/spec/schema files + hashes/SHAs where available;
3. exact source files reviewed;
4. recomputed structural counts;
5. schema PASS/FAIL;
6. graph PASS/FAIL;
7. unit-by-unit semantic findings;
8. blocker classification audit;
9. any unsupported or missing semantics;
10. whether the 38 ready leaves remain 38 after independent review;
11. corrected counts if any statuses change;
12. final exact disposition string;
13. explicit statement that historical full P6.1 aggregate counts were **not** used as generation/QA targets for LTL-03;
14. explicit statement that no protected runtime mutation occurred.

Also append a concise result to `claude_chatGPT.md` so ChatGPT/Owner can resume without repeating the investigation.

---

# 9. HARD STOP CONDITIONS
Claude must STOP and report before any of the following:

- any Supabase write/reseed/row replacement;
- any protected runtime mutation;
- any WorkDefinition persistence generated from this reconstruction;
- any wiring of the full protected tree into production/demo runtime;
- any `main` merge;
- any production promotion;
- any attempt to force the LTL-03 reconstruction to match historical aggregate P6.1 counts;
- any change from P6.1 V1 semantics to AR0.2/VNext semantics;
- any fabrication of missing client or domain knowledge.

After independent QA PASS, the next decision belongs to the Owner: freeze corrected LTL-03 reconstruction, sync Drive, then decide whether to expose protected depth in an admin-only demo surface and/or continue deterministic regeneration across the remaining 21 tasks.

---

## 10. CURRENT STATUS WHEN THIS HANDOFF WAS WRITTEN
- Reconstruction compiler spec V1: frozen on `atlas-p6-1-v1-reconstruction`.
- LTL-03 machine artifact: committed at `39ab0b4995291b3dddd664a8d3d13ad2fdc4a92e`.
- ChatGPT machine/schema/graph self-QA: PASS, evidence committed at `5b87c80fa675af887030016be46b3ddd87f0043d`.
- Current self-QA counts: `64 / 48 / 38 ready / 7 client-binding / 3 knowledge-gap`.
- Independent QA: **NOT YET PASSED**.
- Protected runtime store: **UNTOUCHED by this reconstruction**.
- `main`: **UNTOUCHED**.
- Production: **UNTOUCHED**.

Claude: proceed with the independent QA only, using the exact order above. Do not broaden scope.