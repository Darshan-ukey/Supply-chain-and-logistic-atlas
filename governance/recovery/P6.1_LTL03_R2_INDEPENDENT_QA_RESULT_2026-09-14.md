# P6.1 V1 LTL-03 R2 — Independent Content QA Result

**Reviewer:** Claude (independent — not reusing ChatGPT's self-QA arithmetic or narrative claims)
**Reviewed bundle:** `governance/baselines/p6_1_v1_ltl03_r2/` — manifest `eb06dd9`, parts `573bcc0`/`dbb6dc0`/`23d7794`/`315c662`
**Self-QA reviewed as evidence-to-verify:** `governance/recovery/P6.1_LTL03_R2_REGENERATION_SELF_QA_2026-09-14.md` @ `c07c13e`
**Rule authority applied:** corrected CR1–CR11 @ `87015d7`

Historical LTL-03 (43/37/14/8/15) used as post-hoc comparison only, per instruction — not as a QA target.

---

## 1. Bundle integrity — independently verified, not trusted

Fetched all four part files directly and recomputed their SHA-256 against the manifest's declared hashes: **all 4 match exactly.** Reassembled the full 38-unit artifact from the parts myself (caught and fixed my own reassembly bug along the way — first attempt mis-parsed the part-file wrapper structure and produced 16 "units," corrected before drawing any conclusion from it).

## 2. Structural/schema/graph — independently recomputed

- Schema validation of the reassembled artifact against `canonical-work-decomposition-contract-v1.schema.json`: **the work units themselves are schema-valid.** However, the **manifest's `summary` object fails schema validation** — missing required fields `knowledgeGapCount`, `clientBindingRefCount`, and `executorProof`. This is a genuine, minor conformance gap (the informal manifest summary uses `leafStatusCounts` correctly but omits three schema-required scalar/status fields), not a defect in the work units.
- Graph, recomputed from scratch: 38 units, 32 leaves, 6 internal nodes, exactly 1 root, 0 duplicate IDs, 0 bad parent references, 0 cycles, 0 leaves stuck at `NEEDS_DECOMPOSITION`. **All match the manifest exactly, independently derived.**
- Leaf status counts, recomputed: 14 `EXECUTOR_READY` / 11 `BLOCKED_BY_KNOWLEDGE_GAP` / 7 `BLOCKED_BY_CLIENT_BINDING` — **matches the manifest exactly.**
- Blocker referential integrity: every one of the 18 blocked leaves carries a valid, non-empty blocker reference; all 11 declared `requiredKnowledgeGaps` and all 9 declared `requiredClientBindings` are actually used, each mapping to exactly one leaf, no orphaned or redundant references.

## 3. Structural discipline — CR1/CR2 applied correctly, verified directly

The 20-step `workDecompositionSeed` is preserved as the primary spine (units 01–20, one per seed item). Exactly **5** of the 20 areas are expanded into children — Parties (05), Handling Units (07), Line Items (08), DangerousGoods (09), Instructions/Service Windows (10) — matching the self-QA's claim, and I verified this by walking the actual tree, not by trusting the claim. **DangerousGoods is genuinely consolidated to 2 units** (09A applicability gate, 09B one consolidated family resolution) instead of R1's 6 — the clearest CR2 violation from my original analysis is fixed. This is real, verified discipline, not asserted discipline.

5 additional `X01`–`X05` units represent the explicitly source-named unresolved labels (Code, SHC, Related Value, BOL Type, Reference Number Type) as their own fail-closed leaves rather than folding them into a parent's readiness — correct application of the fail-closed principle.

## 4. Content grounding of the 14 ready leaves — spot-verified, not assumed

Pulled full content for all 14 and checked representative units against source directly:
- `WD-LTL03-R2-09B` (DangerousGoods family resolution) correctly states the family is resolved "as one associated family" — directly echoing the source's own "one associated conditional object family" language, not generic.
- `WD-LTL03-R2-06` (References) correctly separates the *mechanism* of instantiating typed Reference objects (ready) from the *unresolved vocabulary* (`Reference Number Type Full Name`, correctly split out as its own blocked `X05` unit) — a materially better structural decision than R1, which left this ambiguous.
- `WD-LTL03-R2-07A`/`08B` correctly encode the source's own explicit sequencing rules (hierarchy-before-counts; piece-count validated independently of handling-unit count) rather than generic restatement.

No fabricated domain content found in the units checked.

## 5. The one real, unresolved gap: CR10's "not material" determination is asserted, not demonstrated

This is the decisive finding of this review, and I am applying the same standard I applied to the original R1 artifact — a narrative claim of compliance is not evidence of compliance.

The self-QA states: *"Corrected CR10 is applied: policy objects are not treated as actor assignments... Where authority would be material and unresolved, the existing V1 blocker taxonomy applies."* This is a claim about methodology. **I checked the actual artifact content for all 14 ready leaves and found zero explicit authority-materiality determinations anywhere** — no field, no note, nothing recording that authority was considered and found not material for that specific leaf. Corrected CR10 explicitly requires: *"If authority is not material, record that determination; do not manufacture a blocker."* Recording did not happen; only the downstream consequence (no blocker was manufactured) is visible.

**My own assessment of substance, for what it's worth:** the 14 ready leaves are predominantly mechanical object-instantiation/association/capture operations (acquire payload, instantiate objects, associate a candidate value, emit output, capture evidence) rather than decision/precedence-dependent operations — and every genuinely decision/conflict-dependent unit in the tree (03, 14, 16, 17) is correctly blocked. This makes it **plausible that authority genuinely is not material** for most of the 14. But plausibility is not the same as the artifact demonstrating it, and I should not substitute my own inference for the explicit record the corrected rule requires — that would repeat exactly the sequence-inference failure mode CR3/CR4 exist to prevent, just applied to my own review instead of the generation.

## 6. Structural comparison to historical — not a target, noted as forensic context only

Recomputed delta: -5 work units, -5 leaves, 0 ready-count delta, -1 client-binding, -4 knowledge-gap versus historical. The exact match on `EXECUTOR_READY` count (14 = 14) is noted but **not treated as validating evidence** — I checked the actual 14 leaves' *content* against source directly (§4) rather than accepting the count match as reassurance, since an exact match on one dimension while others differ is exactly the kind of thing that could mask either genuine convergence or coincidental tuning. Based on the content review, I have no evidence of count-tuning — the leaves that are ready are ready for source-specific reasons, not because a target was being approached.

---

## Disposition

**Not a clean PASS.** Per the required options, recording exact bounded corrections and stopping before any expansion to the other 21 tasks:

`P6_1_V1_LTL03_R2_INDEPENDENT_QA_FAIL__BOUNDED_CORRECTIONS_REQUIRED`

This is **not** a structural, graph, source-grounding, or over-decomposition failure — those are genuinely sound and independently reconfirmed. It is narrower and specific:

**Required corrections:**
1. Add the three missing required fields (`knowledgeGapCount`, `clientBindingRefCount`, `executorProof`) to the manifest's `summary` object so it validates against the actual schema.
2. For each of the 14 `EXECUTOR_READY` leaves (`WD-LTL03-R2-01`, `04`, `05A`, `06`, `07A`, `08A`, `08B`, `09A`, `09B`, `10A`, `10C`, `12`, `18`, `20`), add an explicit, brief authority-materiality determination — either a stated reason authority is not material to that specific leaf (most plausible: it is a mechanical object-resolution operation, not a decision/precedence operation), or, where genuinely material and unresolved, reclassify to the appropriate existing blocker type per corrected CR10. Do not add a new field/blocker taxonomy beyond what the Contract already defines — a short note within an existing field, or a minimal new schema-compatible field, is sufficient; the requirement is that the determination is recorded, not its exact representation.

**Not required:** any change to the tree's shape, the blocker classifications for the 18 blocked leaves (verified correctly calibrated, §2), or the DangerousGoods consolidation (verified correct, §3).

No Supabase mutation. No WorkDefinition persistence. No main merge. No production promotion. No expansion to the other 21 tasks until these bounded corrections are made and re-verified.
