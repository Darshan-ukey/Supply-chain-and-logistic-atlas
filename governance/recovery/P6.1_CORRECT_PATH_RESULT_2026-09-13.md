# P6.1 Correct-Path Recovery — Result

Executed Steps 1-3 of `P6.1_CORRECT_PATH_HANDOFF_2026-09-14.md`. Reporting per Step 7 before any
further action, as instructed.

## Step 1 — stopped decompression guessing

No further codec attempts made against the current `__ALL_22__` row this round.

## Step 1.5 — cheap check first: does Supabase already have the 22 per-task rows?

```sql
select source_task_id, module_id, module_version, payload_encoding, status, content_hash
from public.atlas_work_decompositions order by source_task_id;
```

**Result: exactly one row.** `source_task_id='__ALL_22__'`, `payload_encoding='BROTLI_BASE64'`,
`content_hash=2c26e760ff6a5b3d4a0500d22f79b531a92fb8380d5b31d2a60b8b49506092ab`. The certified
seeder's expected representation — 22 individual `GZIP_BASE64` rows, one per task — **does not
exist in the table at all.**

## Step 2 — searched Drive for the original private P6.1 bundle

Read `scripts/seed-p6-1-work-decompositions.mjs` @ `ba9d47f07b59ecf79ff6cde9145c0185cc18d39d`
first to get the exact expected input shape (`{moduleId, moduleVersion, decompositions:{...22
entries}}`, passed as a local file path via CLI arg or `ATLAS_WORK_DECOMPOSITION_SEED_FILE`) and
confirmed its own log line: *"No decomposition-detail JSON was written into the web bundle"* — by
design, the raw bundle was never intended to reach GitHub. Drive was therefore the only remaining
place it could plausibly have been archived.

Searches run (all against the full Drive, not a subfolder):
1. `title contains 'P6.1' and title contains 'decomposition'` → only the 2,667-byte human-readable
   "COMPLETE" doc.
2. `title contains 'road-ltl' and title contains '1.5'` → only the P6.0 human-readable doc.
3. `(title contains '22' or 'decompositions' or 'bundle') and (json or zip)` → 4 zip files, none
   named for P6.1. Opened the largest and most temporally-plausible (`atlas-r0-1a-r-custody-
   bundle.zip`, 751,916 bytes, 2026-09-08) — contents are entirely R0.1A **Universe/Page-0**
   semantic-recovery material (`universe-semantic-payload.json`, extraction reports, etc.).
   Unrelated to P6.1 Road LTL decomposition.
4. `modifiedTime` bounded to the exact P6.1 creation date (2026-09-07) across the whole Drive →
   **exactly one file touched that day**: the human-readable "COMPLETE" doc itself.
5. Browsed the doc's parent folder directly (Drive root) → personal/unrelated files only, no Atlas
   data subfolder.
6. `title contains 'seed' or 'private' or fullText contains 'ATLAS_WORK_DECOMPOSITION_SEED_FILE'`
   → zero results.

## Step 3 — candidate verification

No candidate bundle was found to verify. N/A.

## Step 7 — result

**`PRIVATE_BUNDLE_NOT_FOUND__CUSTODY_GAP_CONFIRMED`**

The private Road LTL 1.5 P6.1 decomposition bundle — the actual input to the certified seeder —
does not exist in Drive, and Supabase never received the certified 22-row representation the
seeder would have produced from it. Only the single `__ALL_22__` aggregate row exists, in a form
that does not decode under any tested codec (8 attempts, 2 languages, prior round) despite
confirmed byte-correct transport. Its origin — how or when it was written, by what process, in
place of the certified per-task rows — is not established by this search.

## Not done, per the hard guardrails

- No protected-store mutation.
- No reseed attempt.
- No fabricated decomposition tree or WorkDefinition instance.
- No main merge, no production promotion.
- Frozen historical evidence (the Drive "COMPLETE" doc, the certified GitHub commits) left
  untouched.

## Recommendation

This is now a genuine custody question for the Owner, not a technical retry: either the private
bundle exists somewhere neither Drive nor GitHub search can reach (a local machine, a different
Drive account, an external service), or it was never durably archived and the only surviving
record of P6.1's actual decomposition detail is the human-readable aggregate totals already
public-safe-summarized in `P6_1_PUBLIC_DECOMPOSITION_SUMMARY.json`. Recommend the Owner confirm
whether such a source exists before further recovery effort is spent searching for it.

---

## Follow-up — Owner-directed check for later GitHub updates (2026-09-13)

Owner asked whether any GitHub data dated after 2026-09-07 could close DUX-02 or DUX-03.
Checked exhaustively; neither has a newer-data resolution available.

### DUX-02 — no newer data exists anywhere
Checked full history, across all 37 branches, of every commit touching `data/atlas-registry.json`
and `data/module-catalog.json`. The most recent commit touching either file, on any branch, is
`659177be` — 2026-09-01, "Atlas v2.1.0 RC1 final integrated release" — **older than** the version
already incorporated into the demo branch at D2.0.2. There is no later canonical update sitting
unused anywhere. The defensive cache-busting fix already reflects the most current data that
exists in this repository.

### DUX-03 — finding upgraded, not resolved: the certified production resolver would fail identically
Read `lib/api/work-decomposition.js` at the authoritative certified commit `ba9d47f` (the exact
file ChatGPT's handoff named as ground truth). Its `decodePayload()` for `BROTLI_BASE64` is:
`zlib.brotliDecompressSync(buffer)` — the identical call already tried and already confirmed to
fail against the stored `__ALL_22__` row.

Verified the function itself is not at fault: ran `decodePayload()`'s exact logic against a
known-good brotli sample generated locally — decoded correctly. The failure is isolated to the
stored row's bytes, not the decoding approach or the code.

**This means the certified, production resolver — not just this investigation — would hit the
identical failure if it ever actually served this exact stored row.** The representation itself
(one Brotli aggregate row, `__ALL_22__`) is confirmed as the correct, intentional, certified design
— the repo shows the architecture changed from an earlier per-task-GZIP-row design (2026-09-06,
`scripts/seed-p6-1-work-decompositions.mjs`) to the aggregate-Brotli design actually shipped
(2026-09-07, `lib/api/work-decomposition.js` @ `ba9d47f`). The stored bytes for the one row that
exists are simply not valid brotli data under the certified decoder — a genuine data-integrity
issue in the live protected store, independent of and prior to this demo sprint.

### Updated Step 7 result

`PRIVATE_BUNDLE_NOT_FOUND__CUSTODY_GAP_CONFIRMED` stands, now with additional evidence: even if the
private bundle were located and reseeded were authorized, the CURRENT stored row would need to be
replaced regardless, since it does not decode under the certified production code path either —
not a demo-sprint-specific problem, a live-system one.

### Not done, unchanged
No protected-store mutation. No reseed. No fabrication. No main merge. No production promotion.

### Recommendation, updated
This is worth the Owner's attention independent of the demo: the live `__ALL_22__` protected row
appears genuinely corrupted or was never successfully written as valid brotli, and the certified
resolver (`lib/api/work-decomposition.js`) would return HTTP 500 if it ever actually served this
exact tuple in production today. Recommend this be raised as its own tracked item, separate from
DUX-03's demo-content workaround.
