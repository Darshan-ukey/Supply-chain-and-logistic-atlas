# v0.6.6 Stage-5 Post-build Audit

## Automated regression
18/18 suites PASS:
1. baseline intelligence
2. scenario/client twin
3. intake/adversarial
4. runtime/orchestration
5. review
6. integration/integrity
7. semantic/canonical resolver
8. governance/write protection
9. adaptive density
10. simulation engine
11. handoffs
12. data provenance
13. risk guardrails
14. client twin simulation
15. chat intents
16. UI action validation
17. context/applicability
18. browser/UI contract

## Defects caught during build
- UI action validation initially accepted syntactically valid but nonexistent `LTL-99`; fixed by validating against the model registry.
- Context normalization initially handled array conditions but not singular condition input; fixed and regression-tested.
- Standard journey is now derived from the governed graph rather than a hard-coded shortened list. It contains all 13 STANDARD processes.
- Multiple explicit recovery routes are never auto-selected.

## Frozen integrity
All existing `verifyModelIntegrity()` checks PASS. `public/atlas.html`, the V6.2.3 Constitution and Road LTL model were not changed by v0.6.6.

## JSX/static compile gate
The new React JSX files parse with zero TypeScript parser diagnostics.

## Local Next production compile
NOT EXECUTED / NOT PASSED LOCALLY. The build container cannot resolve `registry.npmjs.org`; `npm install` times out. This remains a mandatory Vercel acceptance gate and is not waived.

## Headless Chromium
The container Chromium process does not complete reliably in this environment, so an actual browser interaction pass is also a mandatory Vercel/live-deployment acceptance gate. Source-level browser/UI contract tests pass; this is not represented as equivalent to a real browser pass.

## Final semantic / visual hardening
- Node swimlanes now explicitly say they represent **node applicability context** and do not imply a live shipment location without telemetry.
- Live execution tokens now animate continuously during Play rather than moving only on the first state change; `prefers-reduced-motion` still disables motion.
- Browser-contract tests assert both safeguards.
