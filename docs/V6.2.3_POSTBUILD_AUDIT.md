# Atlas V6.2.3 + Intelligence v0.6.5 — Post-build Audit

## Executed integrity checks
- Frozen Page 0 V6.2.2 hash: PASS.
- Frozen integrated V6.2.2 + Road LTL V1.2 hash: PASS.
- Public frozen V6.2.2 deployment copy: PASS.
- Road LTL structured model hash: PASS.
- Atlas Constitution V6.2.3 hash: PASS.
- Deployed V6.2.3 additive file hash: PASS.
- **Strip V6.2.3 additive guide block -> exact frozen V6.2.2 integrated hash: PASS.**

## Executed regression suites
- Intelligence baseline: PASS.
- Scenario / Client Twin: PASS.
- Intake / adversarial: PASS.
- Runtime / orchestration: PASS.
- Human Review: PASS.
- Integration / additive integrity: PASS.
- Semantic / canonical / provenance: PASS.

## Semantic coverage verified
- All 22 Road LTL process IDs trace through governed relationships.
- All 220 governed LTL entity nodes resolve back to their governed process.
- All Road-LTL input/output business-object IDs resolve to at least one governed process.
- POD, invoice, claim, BOL and rate canonical trace anchors remain correct.
- Methodology/how-built queries retrieve V6.2.3 Constitution entries.
- `ATLAS CHILD SYNTHESIS` explains the provenance class rather than a source framework.
- Bare/shared `OTHER` without context asks clarification and does **not** invoke the LLM.
- `OTHER_LTL_KNOWLEDGE` with exact/current context resolves to trace relevance, not jurisdiction.
- `Other national / corridor` with exact/current context resolves to jurisdiction, not trace relevance.
- Ambiguous `Warehouse` is allowed to ask whether the user means the operating role, logistics node, actor or another Atlas construct.

## LLM behavior
The provider gateway is retained. System instructions now require practical/plain-English explanation first while keeping classification/provenance/source boundaries explicit. Illustrative examples are permitted only when labelled and may never become Atlas/client facts.

## Known deployment-only check
A real Vercel `next build` remains the final environment check. Source/regression/integrity tests in this package pass before handoff.

## Additional governance/runtime audit
- Governance source-scan suite: PASS.
- Write-capable Atlas agents: 0.
- App/lib runtime source contains no filesystem write/delete primitives: PASS.
- Explainer agent is read-only: PASS.
- Automatic Atlas correction remains deferred/prohibited: PASS.

## Local production-compile limitation
`npm install` was retried in this execution environment and timed out before dependencies could be installed. Therefore a local `next build` is **not** marked as passed. Git/Vercel must perform the final production compile. This is an environment limitation, not silently recorded as a successful build.

## Browser/runtime note
A local headless-Chromium standalone render was attempted but the container browser process stalled on the very large integrated HTML/embedded child and produced no DOM output before timeout. No browser-pass claim is made from that attempt. The Vercel deployment is the authoritative browser/runtime verification step after Git commit.
