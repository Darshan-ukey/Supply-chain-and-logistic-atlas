# v0.6.2 deployment/security patch

Purpose: retain the full v0.6 application, tests and governance artifacts while updating only deployment/runtime dependencies needed for Vercel security acceptance.

Changes:
- Next.js -> 15.5.23 (current 15.x backport line at packaging time)
- React -> 19.1.5
- React DOM -> 19.1.5
- Node engine -> 20.x
- package/runtime version -> 0.6.2
- Full six-suite regression script restored

No intentional changes to frozen Atlas HTML, extracted Road LTL model, agent logic, scenario logic, APIs, client-twin behavior, evidence states or Supabase schema.
