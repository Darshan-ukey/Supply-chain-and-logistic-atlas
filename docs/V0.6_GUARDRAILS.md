# v0.6 integration guardrails retained

The complete integrated Atlas is immutable source content. Temporary UI intelligence only. Client workspace data stays separate. Unknown/reference/inference status cannot be silently promoted. Uploaded evidence requires Trust Gate and human review. Public research remains PUBLICLY_EVIDENCED. LLM uses evidence packets. Integrity hashes must pass before promotion.
