# v0.6.6 Vercel Acceptance Gate

Do not promote the release merely because GitHub upload succeeds.

## Build
- Deployment source is GitHub `main`, not Vercel Drop/redeploy of an old artifact.
- `npm install` completes.
- `npm run build` completes with Next.js 15.5.23.
- Deployment status = Ready.
- No vulnerable-Next rejection.

## Frozen/integrity
Open `/api/integrity` and confirm `ok: true` with all checks true.

## Page 0 UX
- Work/Learn switch visible.
- Work mode collapses Source-native reference models, Directional mapping and Planned execution-model universe.
- Expand restores the original content.
- Select Road LTL / role / movement / node/context; `Apply & collapse context` hides the large selector block.
- Active Context Ribbon correctly shows the selected context.
- `Edit context` restores selectors.
- Refresh preserves presentation preferences.
- `ⓘ` help returns Constitution-grounded selector meaning.

## Simulation Studio
- Opens as main workspace.
- 13-step standard journey shown from LTL-01 to LTL-17.
- Play/Pause/Previous/Next/Reset work.
- Physical/Information/Financial/Controls/Evidence toggles work.
- Actor/System/Node view changes the lane representation.
- LTL-08 shows its explicit branch to LTL-09.
- Exception injection from LTL-08 goes to LTL-14 → LTL-15 and presents LTL-08/LTL-12/LTL-13 recovery choices without auto-selecting.
- Handoff view lists all 39 relationships.
- Data view does not invent payload fields and uses `NOT_MODELED` where appropriate.
- Risk view contains provenance and no unsupported numbers.
- Compare view preserves `REFERENCE_ONLY`/`UNKNOWN` client states.

## Chat
- `explain the page in short` summarizes the current Page 0/Road LTL page.
- It does not answer with an unrelated process/control.
- `what is happening here?` in Simulation Studio explains the active step.
- `what data is exchanged here?` uses the selected handoff.
- `what happens next?` follows deterministic standard relationship.
- `what does OTHER mean?` clarifies when context is ambiguous.
- Chat Minimize/restore works.
- LLM badge shows expected provider; deterministic fallback remains safe if provider fails.

## Security
- Browser source contains no Gemini/Supabase service-role secret.
- Environment variables remain server-side.
- Client workspaces must not be used for production confidential data until auth + tenant-bound RLS is implemented.
