# Supply Chain Operations Intelligence Platform — v1.1.8

**Locally Release-Certified · Governed Page-0 Composition · Road LTL A5 Verified**

v1.1.8 is the governed-composition successor to the exact certified v1.1.7 final GitHub master. It preserves the Stage-23 spatial product, complete V6.2.3 Page 0 + Road LTL V1.2 Reference Atlas, Stage 17–21 client/product layers, Foundation Hardening v1.1, and the eight-function Vercel architecture while adding deterministic Page-0-to-canvas composition.

## v1.1.8 additions

- Full **71-destination Page-0 Navigator** in the modern product surface.
- Four primary canvas entry routes: **Mode / Service, Enterprise, Operating Model, Logistics Node**.
- **711 pairwise + 20 cross-axis** governed compatibility/prerequisite rules.
- Governed Context Composer with direct, conditional, prerequisite, unavailable, research and reference-only states.
- Road LTL **Execution Role / Perspective** refinement kept separate from the 19 Page-0 Operating Models.
- Deterministic **configuration → graph resolver → spatial canvas** pipeline.
- Reverse entry from enterprise domain, operating model or node into compatible modes.
- 27 enterprise lenses and 9 executive cycles retained as analytical views rather than new taxonomy.
- Ask Atlas receives the new composition state and applies context commands through the governed composer; it cannot bypass Page-0 validation.
- Inspector remains canonical-data-driven and adds the governed composed context as an additive section.
- Saved Views capture/restore the v1.1.8 governed context.

## Certified invariants

- Stage-23 / v1.1.7 spatial canvas: 15 territories, ambient mesh, minimap, no boxed territory renderer.
- Semantic zoom remains **Universe → focused A3 → scoped A4 → A5 contract**.
- A5 Inspector, playback, Freeze Time, five synchronized signals, Trace, Actor/System/Control/Source lenses, Compare and Transform remain functional.
- Client AS-IS, discovery, validation, opportunities, TO-BE, collections/presentation/collaboration runtimes remain present.
- Complete legacy **Page 0 V6.2.3** and **Road LTL V1.2** navigation remain accessible as Reference Atlas.
- Canonical Page 0, Road LTL, Enterprise Core, Supply Chain Domain Pack, Atlas Data Contract v1.1 and governance registries remain byte-anchored.
- Stage 17, 19, 20 and 21 are byte-identical to v1.1.7. Stage 18 changes only to integrate the governed v1.1.8 context boundary.
- 8 Vercel serverless functions preserve 28 public API paths.
- Road LTL V1.2 remains the **only A5-verified execution module**. Reference-only modules are never backfilled by the LLM.

## Main commands

```bash
npm run check              # canonical/rules/source/graph/API/security/pilot gates
npm run browser:governed   # governed configuration → graph → canvas matrix
npm run browser:features   # Inspector/Ask/playback/trace/lenses/compare/transform parity
npm run browser:live       # strict desktop/mobile spatial + semantic-zoom certificate
npm run browser:reference  # exact legacy Page0 + Road LTL navigation certificate
npm run release:all        # complete local release gate
npm run release:packages   # build Lab / Stable deployment folders
```

## External go-live gates

This project is **locally release-certified**, not production-certified. Live deployment still requires the exact Vercel Lab E2E gate and, for confidential document testing, the approved live Gemini/provider evaluation. The isolated Supabase restore/PITR rehearsal remains deferred and is not a runtime dependency.

See `RELEASE_V1.1.8.md` and `audits/v1.1.8/`.
