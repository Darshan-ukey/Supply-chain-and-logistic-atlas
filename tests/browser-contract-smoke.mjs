import assert from 'node:assert/strict';import fs from 'node:fs';
const page=fs.readFileSync('app/page.js','utf8'),studio=fs.readFileSync('app/components/SimulationStudio.js','utf8');for(const x of ['Simulation Studio','Work','Learn','ACTIVE CONTEXT','Minimize Atlas Intelligence','How to read Atlas','Apply & collapse context','Edit context'])assert.ok(page.includes(x),x);for(const x of ['Journey','Handoffs','Data','Risks & Controls','Systems & Actors','Compare','Previous','Play','Pause','Source & provenance','Exception simulation','Field-level status'])assert.ok(studio.includes(x),x);assert.match(studio,/prefers-reduced-motion/);assert.match(studio,/data-atlas-moving-token/);assert.match(studio,/Node lanes show the selected node/);assert.match(page,/aria-label="Minimize Atlas Intelligence"/);assert.match(studio,/REFERENCE_ONLY/);console.log('browser-contract-smoke: PASS');
