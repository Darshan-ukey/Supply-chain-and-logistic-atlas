import assert from 'node:assert/strict';import {buildSimulation} from '../lib/simulation/engine.js';
const s=buildSimulation();for(const h of s.allHandoffs){assert.ok(['EXPLICIT_MODEL_IDENTIFIERS','NOT_MODELED'].includes(h.data.fieldLevelStatus));for(const id of h.data.sharedIdentifiers)assert.ok(h.data.producerIdentifiers.includes(id)&&h.data.consumerIdentifiers.includes(id));for(const obj of h.information.sharedObjects)assert.ok(h.information.outputObjects.includes(obj)&&h.information.inputObjects.includes(obj));assert.match(h.data.guardrail,/No EDI\/API payload fields are inferred/i)}for(const p of s.processPackets)assert.match(p.data.fieldLevelGuardrail,/Only explicit identifiers and canonical objects/i);console.log('data-provenance-smoke: PASS');
