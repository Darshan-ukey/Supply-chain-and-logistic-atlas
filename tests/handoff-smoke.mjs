import assert from 'node:assert/strict';import {getModel} from '../lib/atlas/store.js';import {handoffPacket} from '../lib/simulation/engine.js';
const m=getModel();for(const e of m.processFlowEdges){const h=handoffPacket(e);assert.ok(h,e.id);assert.equal(h.edge.id,e.id);assert.equal(h.from.id,e.from);assert.equal(h.to.id,e.to);assert.ok(h.edge.semantic);assert.ok(['EXPLICIT_PROCESS_CONTEXT','NOT_MODELED'].includes(h.physical.status));assert.ok(h.provenance.from.provenance);assert.ok(h.provenance.to.provenance);assert.equal(h.client.guardrail.includes('never promoted'),true)}console.log('handoff-smoke: PASS');
