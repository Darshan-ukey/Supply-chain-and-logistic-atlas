import assert from 'node:assert/strict';import {getModel} from '../lib/atlas/store.js';import {riskSignalsForProcess,buildSimulation} from '../lib/simulation/engine.js';
const m=getModel();for(const p of m.processes)for(const r of riskSignalsForProcess(p)){assert.equal(r.quantified,false);assert.ok(['ATLAS-DERIVED HYPOTHESIS','CLIENT-EVIDENCED'].includes(r.status));assert.doesNotMatch(`${r.title} ${r.explanation}`,/\b\d+%|\$\d|probability\s*[:=]\s*\d/i)}const client=riskSignalsForProcess(m.processes[0],{processId:'LTL-01',status:'CONFIRMED',manualHandoff:'Dispatcher emails a spreadsheet'});assert.ok(client.some(x=>x.status==='CLIENT-EVIDENCED'));const inferred=riskSignalsForProcess(m.processes[0],{processId:'LTL-01',status:'INFERRED',manualHandoff:'Maybe manual'});assert.ok(!inferred.some(x=>x.status==='CLIENT-EVIDENCED'));assert.equal(buildSimulation().guardrails.riskQuantificationRequiresEvidence,true);console.log('risk-guardrail-smoke: PASS');
