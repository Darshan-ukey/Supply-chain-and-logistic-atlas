import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';import {getIntegrityManifest,verifyModelIntegrity} from '../lib/atlas/store.js';
const page=fs.readFileSync('app/page.js','utf8'),m=getIntegrityManifest(),h=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
assert.match(page,/\['work','learn'\]/);assert.match(page,/sourceModels:true,crosswalk:true,navigator:true/);assert.match(page,/ACTIVE CONTEXT/);assert.match(page,/Apply & collapse context/);assert.match(page,/Edit context/);assert.match(page,/atlas-v066-hidden/);assert.match(page,/selected-context-chips/);assert.match(page,/localStorage\.setItem\(PREF/);assert.equal(verifyModelIntegrity().ok,true);assert.equal(h('public/atlas.html'),m.additiveV623.sha256);assert.equal(h('data/atlas-constitution-v6.2.3.json'),m.constitution.sha256);console.log('adaptive-density-smoke: PASS');
