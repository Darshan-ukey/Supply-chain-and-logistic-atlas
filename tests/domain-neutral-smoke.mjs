import fs from'node:fs';import{createRequire}from'node:module';const require=createRequire(import.meta.url),core=require('../engine/domain-neutral-core.js'),fixture=JSON.parse(fs.readFileSync('data/fixtures/accounts-payable-module-v0.1.json','utf8')),road=JSON.parse(fs.readFileSync('data/modules/road-ltl-v1.2.json','utf8'));const ap=core.indexModule(fixture),trace=core.traceObject({...fixture,processes:fixture.processes.map((p,i)=>({...p,inputs:i?['invoice']:[],outputs:['invoice']}))},'invoice'),cmp=core.compareGraphs({processes:fixture.processes},{processes:fixture.processes});const pass=Object.keys(ap.processById).length===7&&trace.length===7&&cmp.changed.length===0&&!fs.readFileSync('engine/domain-neutral-core.js','utf8').match(/shipment|ltl|carrier|physical/i);console.log(pass?'PASS · domain-neutral AP fixture · engineChangesRequiredForAP = 0':'FAIL · domain-neutral core');if(!pass)process.exit(1)
