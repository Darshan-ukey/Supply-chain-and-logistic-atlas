import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';import {getIntegrityManifest,verifyModelIntegrity} from '../lib/atlas/store.js';import {capabilityManifest} from '../lib/runtime/capabilities.js';import {retrieveFullAtlas} from '../lib/atlas/full-search.js';
const h=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex'),m=getIntegrityManifest();assert.equal(h('public/atlas.html'),m.integratedRoadLtl.sha256);assert.equal(verifyModelIntegrity().ok,true);assert.ok(retrieveFullAtlas('canonical entity registry Page 0').length>0);const c=capabilityManifest();assert.equal(c.version,'0.6.4');assert.equal(c.integration.fullAtlas,true);assert.equal(m.automaticAtlasCorrection,false);const page=fs.readFileSync('app/page.js','utf8');assert.match(page,/panelOpen/);assert.match(page,/Minimize Atlas Intelligence/);assert.match(page,/LLM OFF · deterministic fallback/);console.log('integration: PASS');
