import assert from 'node:assert/strict';import {validateUiActions,supportedUiActions} from '../lib/runtime/ui-actions.js';
const a=validateUiActions([{type:'FOCUS_PROCESS',processId:'LTL-08'},{type:'FOCUS_PROCESS',processId:'LTL-99'},{type:'EXECUTE_JAVASCRIPT',code:'alert(1)'},{type:'SET_FLOW_LAYER',layer:'information',enabled:true},{type:'SET_FLOW_LAYER',layer:'secret'},{type:'SELECT_HANDOFF',edgeId:'FLOW-10'},{type:'SELECT_HANDOFF',edgeId:'BOGUS'}]);assert.equal(a.some(x=>x.type==='EXECUTE_JAVASCRIPT'),false);assert.ok(a.some(x=>x.processId==='LTL-08'));assert.ok(!a.some(x=>x.processId==='LTL-99'));assert.ok(a.some(x=>x.type==='SET_FLOW_LAYER'&&x.layer==='information'));assert.ok(!a.some(x=>x.layer==='secret'));assert.ok(a.some(x=>x.edgeId==='FLOW-10'));assert.ok(supportedUiActions().includes('SIMULATION_PLAY'));console.log('ui-action-smoke: PASS');
