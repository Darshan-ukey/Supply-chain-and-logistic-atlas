'use strict';
const {load}=require('./_atlas');
function diagnosticRelevance(text){const {road}=load(),t=String(text).toLowerCase();const vocabulary=[...new Set(road.processes.flatMap(p=>p.label.toLowerCase().split(/[^a-z0-9]+/).filter(w=>w.length>5)))];const matches=vocabulary.filter(w=>t.includes(w));return {score:Math.min(1,matches.length/8),matches:matches.slice(0,20),hint:matches.length>=2?'DOMAIN_RELEVANT':'LOW_SIGNAL'}}
function validateCandidates(raw,text){const {processById}=load(),seen=new Set(),facts=[];for(const f of raw||[]){if(!f||typeof f!=='object')continue;const id=String(f.processId||f.canonicalId||'');if(!processById[id])continue;const quote=String(f.evidenceQuote||'').trim();if(!quote||!String(text).includes(quote))continue;const key=`${id}|${quote}`;if(seen.has(key))continue;seen.add(key);facts.push({factKey:`DF-${Buffer.from(key).toString('base64url').slice(0,18)}`,factType:String(f.factType||'OTHER'),statement:String(f.statement||quote).slice(0,1200),processId:id,canonicalType:'PROCESS',canonicalId:id,canonicalLabel:processById[id].label,evidenceQuote:quote,evidenceLocator:String(f.evidenceLocator||'session-document'),confidence:Math.max(0,Math.min(1,Number(f.confidence)||.5)),extractionMethod:'LLM_STRUCTURED_ATLAS_VALIDATED_V1',mappingStatus:'MAPPED',proposedPatch:{documentFactOnly:true}})}return facts}
module.exports={diagnosticRelevance,validateCandidates};
