'use strict';const {json,body,setCookie,supabase,safeError}=require('./_utils');module.exports=async(req,res)=>{if(req.method!=='POST')return json(res,405,{ok:false});try{const b=await body(req);const d=await supabase('/auth/v1/token?grant_type=password',{method:'POST',body:{email:b.email,password:b.password}});setCookie(res,'atlas_access_token',d.access_token,{maxAge:d.expires_in||3600});if(d.refresh_token)setCookie(res,'atlas_refresh_token',d.refresh_token,{maxAge:30*86400});json(res,200,{ok:true,user:d.user,expiresIn:d.expires_in})}catch(e){json(res,e.status||401,{ok:false,error:safeError(e)})}};
