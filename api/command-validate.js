'use strict';
const {json,body}=require('./_utils');const {load}=require('./_atlas');const ALLOWED=new Set(['SET_CONTEXT','RESET_CONTEXT','SELECT_PROCESS','SET_LENS','START_PLAYBACK','PAUSE_PLAYBACK','STEP_NEXT','STEP_PREVIOUS','TRACE_OBJECT','COMPARE_CONTEXT','OPEN_EVIDENCE','ADD_FINDING']);
module.exports=async(req,res)=>{if(req.method!=='POST')return json(res,405,{ok:false});const b=await body(req),c=b.command||b;const {processById,road}=load();if(!ALLOWED.has(c.type))return json(res,400,{ok:false,valid:false,reason:'Command not allow-listed'});if(c.type==='SELECT_PROCESS'&&!processById[c.processId])return json(res,400,{ok:false,valid:false,reason:'Unknown process'});if(c.type==='TRACE_OBJECT'){const known=new Set(road.processes.flatMap(p=>[...(p.inputs||[]),...(p.outputs||[]),...(p.documentIds||[]),...(p.eventIds||[])]));if(!known.has(c.target))return json(res,400,{ok:false,valid:false,reason:'Trace target is not governed in current module'})}return json(res,200,{ok:true,valid:true,command:c})}
