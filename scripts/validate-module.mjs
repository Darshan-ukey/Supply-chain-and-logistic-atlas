import fs from'node:fs';import path from'node:path';import{createRequire}from'node:module';const require=createRequire(import.meta.url),{validateModule}=require('../engine/module-validator.js');
const file=process.argv[2],depth=process.argv[3]||'A5_VERIFIED';if(!file){console.error('Usage: node scripts/validate-module.mjs <module.json> [depth] [moduleId]');process.exit(2)}const abs=path.resolve(file),module=JSON.parse(fs.readFileSync(abs,'utf8')),reg=JSON.parse(fs.readFileSync('data/atlas-registry.json','utf8')),rel=path.relative(process.cwd(),abs).replaceAll('\\','/'),moduleId=process.argv[4]||reg.modules?.find(x=>x.source===rel)?.id||module.id||'';const result=validateModule(module,{depth,id:moduleId});console.log(JSON.stringify({file:abs,moduleId,...result},null,2));if(!result.valid)process.exit(1);
